export default function CaliforniaScopeNote({ className = "" }: { className?: string }) {
  return (
    <p className={`font-sans text-[12px] text-fog/70 leading-relaxed ${className}`}>
      License monitoring and workers&apos; comp cross-checks are currently available for California-licensed contractors.
      Enforcement intelligence, compliance documents, and incident tracking are available in all 50 states.
    </p>
  )
}
