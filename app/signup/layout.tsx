import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Get Started",
  description: "Create your Protekon account and get workplace compliance documents delivered within 48 hours.",
}

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children
}
